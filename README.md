set up :
extract and navigate to the directory then :
pip install -r requirements.txt      
to run :
python esprit_complete_downloader.py 


the css adding is commemted in the function "save_html_page" ( it makes it very bad :( ) 
the images that are needed for the html are stored in the same location as the .html files , the function should be above "save_html_page" function

also added few libraries in the requirements
